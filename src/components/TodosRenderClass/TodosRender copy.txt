import { useDispatch, useSelector } from 'react-redux';
import { deleteidAction } from '../../redux/actions/action';

export default function TodoRender({ deleteSpecific, datafromdataBase }) {
  const dispatch = useDispatch();
  const items = useSelector(state => state.contacts.items);
  // const deleteSpecificTodo = e => {
  //   // deleteSpecific(e.currentTarget.id);
  //   // const deleteWithId = DBid => {
  //   //   setDataBase(dataBase.filter(elem => elem.id !== DBid));
  //   // };

  //   console.log('e.currentTarget.id: ', e.currentTarget.id);
  //   dispatch(deleteidAction(e.currentTarget.id));
  // };

  const rendernewElem = () => {
    return items.map(elem => {
      return (
        <div key={elem.id}>
          <span>name: {elem.name} </span>
          <span>number: {elem.number} </span>
          <button
            type="button"
            id={elem.id}
            onClick={() => dispatch(deleteidAction(elem.id))}
          >
            Delete
          </button>
        </div>
      );
    });
  };
  return (
    <>
      <h2>Contacts</h2>
      {rendernewElem()}
    </>
  );
}
